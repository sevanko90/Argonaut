namespace Argonaut.Features.Json.Hints;

// Values are the MainWindow toolbar ComboBox item indices - keep in sync.
public enum DateDecodingScheme
{
    Off = 0,
    JsMilliseconds = 1,
    JsSeconds = 2,
    KeepaMinutes = 3
}
